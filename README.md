# 🧠 Mini-TensorFlow: Neural Network from Scratch

A lightweight, fully functional Neural Network built entirely from scratch using only **NumPy** and Math. This project demonstrates a deep understanding of the inner workings of AI, including Forward Propagation, Backpropagation, and Gradient Descent, without relying on high-level libraries like TensorFlow or PyTorch.

## ✨ Features
- **Pure Math & Matrices:** Implements Forward Pass and Backpropagation using raw matrix multiplication.
- **Custom Activation Functions:** Includes `Sigmoid` and `ReLU` functions with their exact mathematical derivatives.
- **Gradient Descent:** Custom implementation of weight and bias updates to minimize Binary Cross Entropy Loss.
- **Visualized Learning:** Tracks and plots the learning curve to visually prove model convergence.

## 🛠️ Technologies Used
- **Python 3.x**
- **NumPy** (Core engine for matrix operations)
- **Matplotlib** (Loss curve visualization)
- **Scikit-Learn** (Used strictly for generating the dummy `make_moons` dataset for testing)

## 🚀 How to Run

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/Ahmed-Saeed-Abdullah-Alshanwany/Mini-TensorFlow.git](https://github.com/Ahmed-Saeed-Abdullah-Alshanwany/Mini-TensorFlow.git)
   cd Mini-TensorFlow


2. **Install the required dependencies:**
```bash
pip install numpy matplotlib scikit-learn

```


3. **Run the Neural Network:**
```bash
python neural_network.py

```

